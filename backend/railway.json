{
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "buildCommand": "composer install",
    "startCommand": "php artisan migrate --force && php artisan db:seed --force && php artisan serve --host=0.0.0.0 --port=8000"
  }
}
